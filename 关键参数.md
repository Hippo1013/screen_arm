# 关键参数

本文记录当前联合测试脚本使用的关键几何参数，主要适用于 `test_v2/demo_face_pose_screen_arm_live_follow_udp_avatar.m`、`test_v3/demo_face_pose_screen_arm_live_follow_udp_avatar.m` 和 `test_v4/demo_face_pose_screen_arm_realtime_servo_udp_avatar.m`。坐标单位均为 `m`，角度控制参数除特别说明外使用 `rad` 或 `rad/s`。

## 坐标系约定

- 仿真世界系来自当前 URDF 根坐标系，即 `desk_link` 所在坐标系。
- 机械臂基座坐标系为 `base_link`。
- 当前 URDF 中 `desk_to_arm_base` 固定关节为：

```text
base_link origin in world = [-0.150, 0.000, 0.740]
```

## 当前记录值

| 参数 | 坐标系 | 当前值 |
| --- | --- | --- |
| 深度相机示意模型中心位置 | `base_link` | `[0.0506, 0.0000, 0.0600]` |
| 深度相机示意模型中心位置，换算到世界系 | world / `desk_link` | `[-0.0994, 0.0000, 0.8000]` |
| 假设人头位置，即 `state.faceCenter` | world / `desk_link` | `[0.6500, 0.0000, 1.0000]` |
| 机械臂末端初始位置，即 `screen_center` | world / `desk_link` | `[0.1870, 0.0000, 1.208487]` |
| 机械臂末端初始位置，即 `screen_center` | `base_link` | `[0.3370, 0.0000, 0.468487]` |
| 名义观看距离，即 `state.viewDistance` | world / `desk_link` | `0.4500` |
| 可接受观看距离范围，即 `state.distanceRange` | world / `desk_link` | `[0.3000, 0.6000]` |

## 脚本版本说明

- `test_v2/demo_face_pose_screen_arm_live_follow_udp.m`：第二版人脸姿态模块 UDP 联合测试基础版。
- `test_v2/demo_face_pose_screen_arm_live_follow_udp_avatar.m`：在基础版上加入 Steve 小人可视化。
- `test_v3/demo_face_pose_screen_arm_live_follow_udp_avatar.m`：离散触发式跟随稳定对照脚本；运动期间继续刷新人脸方向预览，并加入软视角保护。
- `test_v4/demo_face_pose_screen_arm_realtime_servo_udp_avatar.m`：实时视觉伺服式连续跟随脚本；每拍读取最新 UDP 姿态，用 DLS 差分 IK 小步更新关节角。

以上脚本都不使用 `face_pose_module_v2` 输出的 `center` 作为真实人头位置，而是将固定点 `[0.6500, 0.0000, 1.0000]` 作为人头/人脸参考点，只使用 v2 输出的 `normal` 和 `imu.accel` 更新朝向。

## v4 实时伺服控制参数

`test_v4` 第一版参数保持保守，目标是先验证连续输入下的稳定响应：

| 参数 | 当前值 | 单位 | 说明 |
| --- | --- | --- | --- |
| `state.controlPeriod` | `0.04` | `s` | 单一 MATLAB timer 周期，约 `25 Hz` |
| `state.renderEvery` | `2` | 拍 | 机械臂 `show(...)` 每 2 拍重绘一次 |
| `state.servoKpOri` | `1.5` | `1/s` | 姿态误差比例增益 |
| `state.servoKpPos` | `2.0` | `1/s` | 位置误差比例增益 |
| `state.qdotMaxRev` | `deg2rad(70)` | `rad/s` | 旋转关节限速 |
| `state.qdotMaxPris` | `0.15` | `m/s` | 棱柱关节限速，由 `jointInfo.types == "prismatic"` 自动分配 |
| `state.lambdaMin` | `1e-3` | - | DLS 阻尼下限 |
| `state.lambdaMax` | `0.05` | - | 奇异附近 DLS 最大阻尼 |
| `state.manipW0` | 运行时自标定 | - | 首次按 `0.15 * w` 估计可操作度阈值 |
| `state.normalDeadbandRad` | `deg2rad(1.0)` | `rad` | 目标法向量死区 |
| `state.normalSlerpAlphaMin` | `0.15` | - | 小变化 slerp 系数 |
| `state.normalSlerpAlphaMax` | `0.55` | - | 大变化 slerp 系数 |
| `state.normalSlerpAngleFull` | `deg2rad(20)` | `rad` | 达到该夹角时使用最大 slerp 系数 |
| `state.normalMaxRateRad` | `deg2rad(120)` | `rad/s` | 目标法向量最大角速度 |
| `state.pitchLpAlpha` | `0.20` | - | IMU 俯仰角 EMA 系数 |
| `state.holdTimeoutSec` | `0.5` | `s` | 丢帧保持/信号暂失提示阈值 |
| `state.watchStuckLimit` | `25` | 拍 | 约 1 秒持续不收敛后触发一次粗到位恢复 |

v4 的雅可比行序按 MATLAB `geometricJacobian` 约定使用 `[角速度(3); 线速度(3)]`，位姿误差和期望空间速度也必须保持同样顺序。

## 现实测试摆放参考

根据当前仿真中固定的相机中心和假设人头中心：

```text
cameraCenterWorld = [-0.0994, 0.0000, 0.8000]
faceCenterWorld   = [ 0.6500, 0.0000, 1.0000]
```

现实测试时，测试人员的人脸中心相对实际相机中心应大致满足：

| 参数 | 当前值 | 说明 |
| --- | --- | --- |
| 水平方向前方距离 | `0.7494 m` | 人脸中心在相机前方约 `75 cm` |
| 左右偏移 | `0.0000 m` | 人脸中心尽量与相机左右对齐 |
| 竖直高度差 | `0.2000 m` | 人脸中心比相机中心高约 `20 cm` |
| 相机中心到人脸中心直线距离 | `0.7756 m` | 约 `78 cm` |
| 建议相机上仰角 | `14.95 deg` | 约 `15 deg`，用于让相机光轴大致对准人脸中心 |

## 来源说明

深度相机示意模型的中心位置来自脚本中的 `depthCameraCenterWorld()`：

```text
cameraBackPlaneInBase = [0.038, 0.000, 0.060]
cameraDepth = 0.0252
cameraForwardInBase = [1, 0, 0]
cameraCenterInBase = cameraBackPlaneInBase + 0.5 * cameraDepth * cameraForwardInBase
                   = [0.0506, 0.0000, 0.0600]
```

假设人头位置来自：

```text
state.faceCenter = [0.65, 0.00, 1.00]
```

机械臂末端初始位置按当前 URDF 和脚本初始关节配置计算：

```text
state.endEffector = "screen_center"
state.q = displayPoseToConfig([0, -120, 120, 30, 0, 0]).'
```

也就是：

```text
q = [0 rad, -2.094395 rad, 2.094395 rad, 0.030 m, 0 rad, 0 rad]
```

计算方式为：

```matlab
T = getTransform(robot, q, "screen_center");
screenCenterWorld = T(1:3, 4).';
```
