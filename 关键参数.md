# 关键参数

本文记录 `test_v2/demo_face_pose_screen_arm_live_follow_udp.m` 当前版本中联合测试使用的关键几何参数。坐标单位均为 `m`。

## 坐标系约定

- 仿真世界系来自当前 URDF 根坐标系，即 `desk_link` 所在坐标系。
- 机械臂基座坐标系为 `base_link`。
- 当前 URDF 中 `desk_to_arm_base` 固定关节为：

```text
base_link origin in world = [-0.150, 0.000, 0.740]
```

## 当前记录值

| 参数 | 坐标系 | 当前值 |
| --- | --- | --- |
| 深度相机示意模型中心位置 | `base_link` | `[0.0506, 0.0000, 0.0600]` |
| 深度相机示意模型中心位置，换算到世界系 | world / `desk_link` | `[-0.0994, 0.0000, 0.8000]` |
| 假设人头位置，即 `state.faceCenter` | world / `desk_link` | `[0.6500, 0.0000, 1.0000]` |
| 机械臂末端初始位置，即 `screen_center` | world / `desk_link` | `[0.1870, 0.0000, 1.208487]` |
| 机械臂末端初始位置，即 `screen_center` | `base_link` | `[0.3370, 0.0000, 0.468487]` |

## 现实测试摆放参考

根据当前仿真中固定的相机中心和假设人头中心：

```text
cameraCenterWorld = [-0.0994, 0.0000, 0.8000]
faceCenterWorld   = [ 0.6500, 0.0000, 1.0000]
```

现实测试时，测试人员的人脸中心相对实际相机中心应大致满足：

| 参数 | 当前值 | 说明 |
| --- | --- | --- |
| 水平方向前方距离 | `0.7494 m` | 人脸中心在相机前方约 `75 cm` |
| 左右偏移 | `0.0000 m` | 人脸中心尽量与相机左右对齐 |
| 竖直高度差 | `0.2000 m` | 人脸中心比相机中心高约 `20 cm` |
| 相机中心到人脸中心直线距离 | `0.7756 m` | 约 `78 cm` |
| 建议相机上仰角 | `14.95 deg` | 约 `15 deg`，用于让相机光轴大致对准人脸中心 |

## 来源说明

深度相机示意模型的中心位置来自脚本中的 `depthCameraCenterWorld()`：

```text
cameraBackPlaneInBase = [0.038, 0.000, 0.060]
cameraDepth = 0.0252
cameraForwardInBase = [1, 0, 0]
cameraCenterInBase = cameraBackPlaneInBase + 0.5 * cameraDepth * cameraForwardInBase
                   = [0.0506, 0.0000, 0.0600]
```

假设人头位置来自：

```text
state.faceCenter = [0.65, 0.00, 1.00]
```

机械臂末端初始位置按当前 URDF 和脚本初始关节配置计算：

```text
state.endEffector = "screen_center"
state.q = displayPoseToConfig([0, -120, 120, 30, 0, 0]).'
```

也就是：

```text
q = [0 rad, -2.094395 rad, 2.094395 rad, 0.030 m, 0 rad, 0 rad]
```

计算方式为：

```matlab
T = getTransform(robot, q, "screen_center");
screenCenterWorld = T(1:3, 4).';
```
