# 后续开发方向

## 真正实时跟随人脸方向向量

记录日期：2026-06-19

实现状态：已落地第一版，入口为 `test_v4/demo_face_pose_screen_arm_realtime_servo_udp_avatar.m`。`test_v3` 保留为离散触发式稳定对照组。

### 当前背景

当前 `test_v3/demo_face_pose_screen_arm_live_follow_udp_avatar.m` 的跟随逻辑本质上仍然是“离散触发式跟随”：

1. MATLAB 从 UDP 中读取当前最新的人脸姿态帧。
2. 当目标变化超过阈值且机械臂没有运动时，锁定这一帧的人脸方向向量。
3. 基于这一个锁定目标计算屏幕目标位姿。
4. 用逆运动学求解目标关节角。
5. 用关节空间插值轨迹让机械臂移动到目标位姿。
6. 到达后，再根据新的最新姿态决定是否执行下一次移动。

这种方式稳定、容易诊断，也能避免机械臂跟随人脸轻微抖动，但缺点是机械臂动作会呈现“一下一下”的机械式跟随。运动过程中收到的新人脸姿态只用于刷新人物头部和方向箭头，不会改变当前这条已经生成的轨迹。

### 用户设想

后续希望设计一个能够真正“实时”跟随人脸方向向量的方案。

核心目标是：

- 人脸姿态模块持续输出人脸方向向量。
- 机械臂不再每次锁定一个目标点后完整走完一条轨迹。
- 屏幕末端应该随着人脸方向向量的连续变化而连续调整位置和姿态。
- 机器人应始终尽量保持屏幕正对人脸，并维持合适观看距离。
- 需要避免因为人脸姿态估计的轻微抖动导致机械臂高频颤动。

### 初步方案

建议把后续实时跟随版本设计成“视觉伺服式连续控制”，而不是当前的“目标触发式 IK + 轨迹规划”。

整体思路从：

```text
收到新目标 -> 判断变化够不够大 -> IK 求一个目标关节角 -> 插值轨迹走过去 -> 到达后再看下一个目标
```

改为：

```text
持续接收最新人脸方向 -> 持续更新目标位姿 -> 每个控制周期只走一小步 -> 下一周期再根据最新目标修正
```

也就是说，机器人不再追逐一帧固定目标，而是在每个控制周期内根据最新目标位姿做小幅闭环修正。

### 推荐架构

#### 1. 感知输入层

继续沿用 `face_pose_module_v2` 通过 UDP 发送人脸姿态。

MATLAB 端每次读取 UDP 时，只保留缓冲区中的最新数据包，丢弃旧包，避免机械臂追逐过期姿态。

联合测试脚本需要继续提取以下关键字段：

```text
t
valid
normal
imu.accel
```

其中：

- `normal` 用于描述相机坐标系下的人脸方向。
- `imu.accel` 用于估计相机俯仰角。
- `t` 用于判断数据是否为新帧。
- `valid` 用于过滤无效人脸姿态。

#### 2. 坐标变换层

当前测试仍可沿用已有约定：

- 深度相机位置固定为仿真模型中相机示意模型的中心点。
- 相机 `yaw` 默认端正，即镜头朝世界坐标系 `+X` 方向。
- 相机 `roll` 默认无横滚。
- 相机 `pitch` 由 IMU 返回的俯仰信息决定。

将相机坐标系下的人脸方向向量变换到世界坐标系：

```text
normal_camera -> normal_world
```

当前固定人头/人脸参考点仍可先使用：

```matlab
faceCenter = [0.65, 0.00, 1.00];
```

目标屏幕中心点可按：

```matlab
screenTarget = faceCenter + distance * normal_world;
```

其中 `distance` 初始仍优先使用 `0.45 m`，允许范围可继续使用当前扩展后的：

```matlab
[0.30, 0.60]
```

#### 3. 姿态滤波层

实时跟随不能直接使用原始人脸法向量，否则人脸建模的小抖动会变成机械臂末端抖动。

建议加入目标滤波：

- 对方向向量做低通滤波或球面插值。
- 小于死区阈值的变化直接忽略。
- 中等变化平滑跟随。
- 大幅变化允许更快响应，但限制最大角速度。

可采用如下概念：

```text
normal_filtered = slerp(normal_filtered, normal_latest, alpha)
```

其中 `alpha` 可以根据变化角度动态调整：

- 变化很小：`alpha` 较小，抑制抖动。
- 变化较大：`alpha` 较大，提高响应速度。

#### 4. 实时控制层

这是与当前 v3 最大的区别。

当前 v3 是：

```text
完整 IK -> 完整轨迹 -> 执行完再看新目标
```

实时版本建议改为：

```text
当前关节角 q
当前屏幕位姿 T_current
最新目标位姿 T_target
计算位姿误差 error
通过雅可比或差分 IK 求一个小的关节增量 dq
限制 dq 和关节速度
更新 q = q + dq
刷新仿真
```

可选实现路线：

1. 使用阻尼最小二乘雅可比伪逆做差分 IK。
2. 使用 MATLAB 的差分逆运动学能力。
3. 使用短步长 `inverseKinematics`，但每次只允许小幅移动，不再规划完整轨迹。

控制周期建议先从：

```text
20 ms - 50 ms
```

开始测试，即约 `20 Hz - 50 Hz` 的控制频率。

渲染频率可以低一些，例如：

```text
10 Hz - 20 Hz
```

这样可以避免 MATLAB 图形刷新成为主要性能瓶颈。

#### 5. 限幅与安全层

实时跟随必须加入限幅，否则目标突然跳变时机械臂动作会不稳定。

建议至少包含：

- 单周期最大关节角变化限制。
- 单关节速度限制。
- 关节限位检测。
- 目标不可达时的退化策略。
- 奇异位形附近的阻尼增强。
- 目标距离约束，保持屏幕中心到人脸中心距离在 `[0.30, 0.60] m` 内。

当目标不可达时，不应突然停止整个系统，而应退化为：

- 尽量对准方向；
- 尽量保持距离；
- 尽量减少位置误差；
- UI 中提示当前目标已进入低可达性区域。

#### 6. 显示与控制解耦

实时控制版本中，仿真显示不应阻塞控制逻辑。

建议：

- UDP 接收和目标状态更新尽量轻量。
- 控制循环只更新关节角和必要状态。
- 机器人模型绘制降频执行。
- 人物头部和方向箭头可以比机器人模型更高频更新。

这样可以减少当前 MATLAB `show(...)` 重绘机械臂带来的卡顿。

### 与当前 v3 的关系

建议保留 `test_v3` 作为稳定对照组。

已新建：

```text
test_v4/
```

用于实现真正实时跟随版本。

其中：

- `test_v3`：离散触发式跟随，适合验证目标位姿、IK 可达性、距离 fallback、UI 诊断。
- `test_v4`：实时视觉伺服式跟随，适合验证连续人脸姿态输入下的动态响应和稳定性。

### 已实现的 v4 第一版

`test_v4/demo_face_pose_screen_arm_realtime_servo_udp_avatar.m` 已实现：

- 单一 `timer`，`Period = 0.04 s`，`BusyMode = "drop"`。
- 每拍读取 UDP 最新帧，只处理时间戳更新且 `valid=true` 的姿态。
- MATLAB 端对人脸法向量做 slerp 死区、角速度限速和自适应插值。
- IMU 俯仰角做 EMA 低通，避免加速度噪声直接驱动末端抖动。
- 目标位姿继续使用 `buildTargetTformFromNormal`，保持屏幕严格正立。
- 伺服内核使用 DLS 雅可比速度控制，行序为 `[角; 线]`。
- 关节限速按 `movingJointInfo(robot)` 的关节类型自动区分旋转关节和棱柱关节。
- 接管时先用完整 IK 粗到位，再进入连续伺服；看门狗在持续不收敛时排队触发一次粗到位恢复。
- 机械臂重绘降频，人物头部和目标 overlay 每拍轻量刷新。
- 无相机测试发包器位于 `test_v4/tools/fake_pose_sender.py`。

已验证：

```powershell
matlab -batch "cd('E:\robotics\final_project\ws'); issues = checkcode('test_v4\demo_face_pose_screen_arm_realtime_servo_udp_avatar.m','-id'); if ~isempty(issues), disp(issues); error('checkcode reported issues'); end"
```

```powershell
matlab -batch "cd('E:\robotics\final_project\ws'); addpath('test_v4','-begin'); demo_face_pose_screen_arm_realtime_servo_udp_avatar('normal', false); pause(1.0); fig = evalin('base','realtimeServoFigure'); if isvalid(fig), close(fig); end"
```

### 初步结论

真正实时跟随已经通过 v4 第一版验证为可落地；后续不应简单地把当前 v3 的 IK 规划频率提高。那样会造成大量重复 IK、图形重绘和目标抖动放大，反而更容易卡顿。

更合理的方向是：

```text
UDP 最新帧输入
+ 人脸方向滤波
+ 世界坐标变换
+ 目标位姿连续更新
+ 差分 IK / 速度伺服
+ 关节速度与限位约束
+ 低频渲染
```

这会把系统从“机械式一步一跟随”升级为“连续闭环跟随”。
